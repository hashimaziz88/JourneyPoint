# JourneyPoint Constitution Pointer

The authoritative constitution for JourneyPoint lives at
[`\.specify/memory/constitution.md`](./memory/constitution.md).

This pointer exists because some agent setups look for
`\.specify/constitution.md` directly. Always prefer the file in
`\.specify/memory/constitution.md` when interpreting governance rules.
